- name: Gather facts about all Users
  oneview_user_facts:
    config: "{{ config }}"

- debug: var=users

- name: Gather facts about a User by name
  oneview_user_facts:
    config: "{{ config }}"
    name: "HPAdmin"

- debug: var=users